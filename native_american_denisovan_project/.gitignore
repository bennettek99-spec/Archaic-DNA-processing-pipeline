# Local paths and data — never commit
config/paths.yaml
data/raw/
data/masks/

# Large regenerable AADR inventory dumps (rebuild with scripts/01_inventory_aadr.py).
# Table 1 (results/tables/table1_sample_inventory.tsv) is the curated deliverable kept.
data/manifests/aadr_inventory_full.tsv
data/manifests/aadr_inventory_relevant.tsv

# AADR data is machine-local and huge; never bundle
*.geno
*.snp
*.ind
*.anno

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.coverage

# Large/intermediate outputs (regenerable from scripts)
results/logs/*.tmp
tmp/
