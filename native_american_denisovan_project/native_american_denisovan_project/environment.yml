name: na-denisovan
channels: [conda-forge]
dependencies:
  - python=3.11
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - pyyaml
  - msprime
# Note: this module reuses the validated `archaic.*` package from the sibling
# archaic-introgression repo (added to sys.path by each script), so it does not
# re-declare that code. R/admixtools2 is OPTIONAL (Tier 14 qpAdm cross-check).
